interface:
  display_name: "Conductor"
  short_description: "Manager session for delegated multi-agent work: auto/strict modes, serial batches with acceptance fences, on-disk state"
  default_prompt: "Use $conductor to coordinate this goal as the manager session. Confirm auto or strict mode first, plan the batches, and delegate across independent agents."
